import { PortfolioComponent } from './portfolio.component';

describe('PortfolioComponent', () => {
  it('should create', () => {
    const comp = new PortfolioComponent();
    expect(comp).toBeTruthy();
  });
});
