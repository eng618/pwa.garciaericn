import { DetailedCardComponent } from './detailed-card.component';

describe('DetailedCardComponent', () => {
  it('should create', () => {
    const comp = new DetailedCardComponent();
    expect(comp).toBeTruthy();
  });
});
