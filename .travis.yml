language: node_js
node_js:
  - "6.9"

cache: yarn

before_script:
  - yarn add global firebase-tools
  - yarn add global @angular/cli

script:
  - ng build --prod

after_success:
  - ./deploy.sh

notifications:
  email:
    on_failure: $EMAIL
    on_success: $EMAIL
